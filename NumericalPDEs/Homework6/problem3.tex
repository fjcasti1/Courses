\begin{questions}

\question{Show that Burgers' equation $u_t + u u_x = \nu u_{xx}$ with
$u(x,t=0) = u_l,~ x < 0$ and $u(x,t=0) = u_r < u_l,~ x > 0$ has a
traveling wave solution of the form $u(x, t) = w(x - s t)$ by deriving
an ODE for $w$ and showing that the ODE is solved by
\begin{displaymath}
	w(y) = u_r + \frac{1}{2} (u_l - u_r) \left[ 1 - 
	\tanh \frac{(u_l - u_r) y}{4 \nu} \right]
\end{displaymath}
where $s = (u_l + u_r)/2$.
}
\begin{solution}
\end{solution}
\end{questions}
